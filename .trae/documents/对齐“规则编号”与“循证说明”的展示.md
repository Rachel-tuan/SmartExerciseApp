结论与建议
- 目前不需要修改“规则编号”。您看到的“规则编号”是当前处方实际应用到的规则集合；而“循证说明”弹窗展示的是常见规则库（八类），两者并非一一对应，容易造成理解偏差。
- 为避免混淆，建议将“循证说明”改为按“当前处方规则”动态展示；若需要查看完整规则库，再提供切换入口。

证据与代码位置
- Admin 循证弹窗数据源：使用 listRulesMeta() 列出全量规则库，见 src/pages/AdminPage.jsx:327-343。
- Admin 处方摘要中的“规则编号”：来自 latestPrescription.rules，见 src/pages/AdminPage.jsx:371-377。
- 处方规则与循证元数据：规则定义在 src/engine/prescriptionEngine.ts:161-375；循证元数据 RULES_META 在 src/engine/prescriptionEngine.ts:378-391；辅助访问函数 getRuleMetaById 在 src/engine/prescriptionEngine.ts:393-401。

实施计划
1) 合并循证数据到单一数据源
- 在 PRESCRIPTION_RULES 每条规则对象中补充 evidence 字段（string[]），从现有 RULES_META 对应条目复制；对没有元数据的规则保持为空或省略。
- 删除 RULES_META 常量，重构以下访问函数仅依赖 PRESCRIPTION_RULES：
  - getRuleMetaById：直接从规则对象返回 {id, name, source, evidence}。
  - listRulesMeta：遍历 PRESCRIPTION_RULES 返回 {id, name, priority, evidence}。
  - getRulePriorityById：从规则对象读取 priority。

2) 对齐 Admin 的“循证说明”与“规则编号”
- 将“规则循证说明”弹窗的数据源改为：
  - dataSource = (latestPrescription?.rules || []).map(id => getRuleMetaById(id)).filter(Boolean)
  - 标题改为“当前处方循证说明”。
- 增加一个切换入口（如按钮）：“查看常见规则库”，点击后展示全量 listRulesMeta() 列表。

3) 一致性与验证
- 统一 PrescriptionPage.jsx 的“规则编号”展示，与 Admin 处方摘要保持来源一致（latestPrescription.rules）。
- 运行构建与测试，确保：
  - 处方生成 generatePrescription 与 CRF 融合不受影响（src/engine/prescriptionEngine.ts:12-67、487-501）。
  - 审计导出（buildAuditMarkdownWithEvidence）继续通过 getRuleMetaById 获取 evidence。

交付物
- 修改 prescriptionEngine.ts 以消除 RULES_META 并合并 evidence。
- 修改 AdminPage.jsx 的循证弹窗逻辑及标题/切换入口。
- 回归构建与测试通过。

请确认以上方案，我将按此实施并提交对应改动。